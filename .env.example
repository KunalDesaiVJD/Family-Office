# Family Wealth OS — environment template
# Copy to .env.local and fill locally. NEVER commit real values.
# This foundation build uses mock data only; the keys below are placeholders
# for future connector-based integrations and are intentionally empty.

# --- Platform ---
NEXT_PUBLIC_APP_NAME="Family Wealth OS"
NEXT_PUBLIC_DEFAULT_TENANT="vj-desai-family"

# --- Future: multi-tenant database (not connected yet) ---
DATABASE_URL=""

# --- Future: authentication provider (not connected yet) ---
AUTH_SECRET=""
AUTH_ISSUER_URL=""

# --- Future: connector integrations (do NOT store real broker/bank creds here) ---
# Real credentials must flow through a secure vault / connector service, never .env.
BROKER_CONNECTOR_BASE_URL=""
TALLY_CONNECTOR_BASE_URL=""

# --- Future: AI Desk (not connected yet) ---
ANTHROPIC_API_KEY=""
